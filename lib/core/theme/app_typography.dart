/// Tokens typographiques — spec `FLUTTER_PROTOTYPE_SPEC.md` §1 & §14.
///
/// - `ui` : Inter pour toute la copy UI.
/// - `display` : Playfair Display pour les titres d'événements (hero, cartes).
abstract class AppTypography {
  AppTypography._();

  static const String ui = 'Inter';
  static const String display = 'Playfair Display';
}